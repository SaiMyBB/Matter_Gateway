# ==========================================
# Matter Gateway Configuration (Example)
# ==========================================

# ---- Larnitech API Configuration ----
# Base REST endpoint for API2
LARNITECH_URL=https://1876d100.in.larnitech.com:8443/api

# Secure WebSocket endpoint for real-time updates
LARNITECH_WS_URL=wss://1876d100.in.larnitech.com:8443/api

# Authentication token (replace with your real one)
LARNITECH_TOKEN=Bearer 3673129911629553

# Serial and password for QR-based LAN auth (optional fallback)
LARNITECH_SERIAL=1876d100
LARNITECH_PASSWORD=11111111

# Retry interval for reconnecting to Larnitech WS (seconds)
LARNITECH_WS_RECONNECT=10


# ---- HomeKit Bridge Configuration ----
# PIN used to pair the HomeKit bridge with the Apple Home app
HAP_PIN=031-45-154

# Polling interval (in seconds) to refresh device states from Larnitech
HAP_POLL_INTERVAL=5


# ---- Gateway Runtime Settings ----
# Logging verbosity (DEBUG, INFO, WARNING)
LOG_LEVEL=INFO

# Local persistence file for virtual device states
DEVICE_STATE_FILE=data/devices_state.json

# Whether to auto-start Larnitech WS listener on launch (true/false)
ENABLE_LARNITECH_WS=true

# Optional: custom port for dashboard (default: 8000)
GATEWAY_PORT=8000
